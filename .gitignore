# Byte-compiled / optimized / DLL files
__pycache__/
*.pyc
*.pyo
*.pyd

# Jupyter Notebook checkpoints
.ipynb_checkpoints

# OS generated files
.DS_Store

scratch/
.admin_config.json
calvert_tester_logs.db
CALVERT_FORECASTER_REVIEW.md

# Research archive — kept in Gitea only (private), not in GitHub (public)
Pittsburgh Data/
Louisville Data/
Odor_Complaint_Analysis_Walkthrough.md

# Generated forecast data (produced by generate_site.py / CI)
docs/data/

# Ad trigger runtime state (produced by ad_trigger.py)
ad_state.json
ad_decisions.log

# El Toro secrets & pre-provisioned order-line mapping — NEVER commit these.
# Templates (.env.example, eltoro_order_lines.example.json) ARE committed.
.env
.env.local
eltoro_order_lines.json

# Large training-data CSVs — stored in Gitea only (too large for GitHub LFS quota).
# Re-generate: run fetch_weather_for_zips_pittsburgh.py + merge_smell_weather_pittsburgh.py
# BLH backfill: Pittsburgh Data/backfill_blh_era5.py (requires ~/.cdsapirc)
Pittsburgh Data/open-meteo-complete_hourly.csv
Pittsburgh Data/open-meteo-smell-merged.csv
